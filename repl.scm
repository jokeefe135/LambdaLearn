(define (repl)
  (load "parser.scm")
  (load "interpreter.scm")
  (display "LambdaLearn REPL v1")
  (newline)
  (let loop ()
    (display "> ")
    (flush-output)
    (let ((line (read-line)))
      (cond
       ((eof-object? line)
        (display "Goodbye!")
        (newline))
       ((string=? line "")
        (loop))
       (else
        (let* ((expr   (parse line))
               (result (g:eval expr)))
          (write result)
          (newline)
          (loop)))))))
