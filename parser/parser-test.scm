;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;; Tests TODO: make test cases
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; (pp (parse "λf:((Nat->Bool)->(Nat->Nat))->Nat. λg:Nat->Bool. λx:Nat. f g x"))
;; (pp (parse "(λk. (λm. m m) (λm. m m)) (λn. k (n n))"))
;; (pp (parse "F = λx.x"))